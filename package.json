{
	"name": "root",
	"private": true,
	"dependencies": {},
	"scripts": {
		"build": "lerna run build",
		"build.release": "lerna run clean && lerna run build",
		"test": "lerna run test",
		"lint": "lerna run lint",
		"format": "lerna run format",
		"clean": "lerna run clean"
	},
	"devDependencies": {
		"lerna": "^8.1.2"
	}
}
